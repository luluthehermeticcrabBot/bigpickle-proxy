services:
  bigpickle-proxy:
    build: .
    ports:
      - "8000:8000"
    command: ["cloud"]
    restart: unless-stopped

  # Optional: serve mode with local OpenCode (requires OpenCode installed in image)
  # bigpickle-proxy-serve:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   command: ["serve"]
  #   restart: unless-stopped
